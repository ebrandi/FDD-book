# Makefile for Chapter 6, Lab 2: Minimal Module with Logs Only.
#
# FreeBSD's kernel-module build infrastructure handles the heavy
# lifting. The only variables you set are the module name and the
# list of sources.

KMOD=	hello
SRCS=	hello.c

.include <bsd.kmod.mk>
