# Makefile for Chapter 6, Lab 4, Experiment 1A (DELIBERATELY BROKEN).
#
# Only load this module in a lab VM. The module leaks /dev/demo on
# unload; reboot the VM to clean up.

KMOD=	demo
SRCS=	demo.c

.include <bsd.kmod.mk>
