# Makefile for detectmod - Chapter 30 Lab 2 (Hypervisor Detection).
#
# A single source file kernel module that reads vm_guest and logs the
# detected environment at load time.

KMOD=	detectmod
SRCS=	detectmod.c

SYSDIR?= /usr/src/sys

.include <bsd.kmod.mk>
