# Lab 10: Multi-Version Compatibility Matrix
#
# Builds mfctl-universal, a single user-space control program that
# works against three driver versions (1.6, 1.7, 1.8) via the
# three-tier capability fallback.

PROG=		mfctl-universal
SRCS=		mfctl-universal.c
NO_MAN=		yes
MK_WERROR=	no

.include <bsd.prog.mk>
